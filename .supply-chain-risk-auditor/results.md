# Supply Chain Risk Report

---

## Metadata

- **Scan Date**: 2026-04-03
- **Project**: Nudgy (macOS notification companion for AI coding agents)
- **Repositories Scanned**: 0 third-party Swift package repositories; 1 GitHub Actions dependency
- **Total Dependencies**: 0 third-party runtime dependencies; 1 CI-only dependency (`actions/checkout@v4`)
- **Scan Duration**: Manual audit

---

## Executive Summary

Nudgy has an exceptionally minimal supply chain attack surface. The project declares **zero third-party Swift package dependencies** in its `Package.swift`. There is no `Package.resolved` file (it is gitignored, consistent with the empty dependency array). No alternative dependency mechanisms (CocoaPods, Carthage, vendored code, xcframeworks) are present. The project relies exclusively on Apple first-party frameworks (SwiftUI, AppKit, Foundation, Network, etc.) that ship with macOS.

The only external dependency in the entire project is `actions/checkout@v4` used in the GitHub Actions CI workflow (`.github/workflows/ci.yml`). This is an official GitHub-maintained action (~7,700 stars, organization-backed, not archived) and is considered low-risk.

The project has a `SECURITY.md` with a responsible disclosure policy and a security contact email.

**Overall supply chain risk posture: Very Low.** There are no high-risk dependencies to report.

### Counts by Risk Factor

| Risk Factor              | Dependencies | Total |
|--------------------------|--------------|-------|
| Single maintainer        | (none)       | 0     |
| Unmaintained             | (none)       | 0     |
| Low popularity           | (none)       | 0     |
| High-risk features       | (none)       | 0     |
| Past CVEs                | (none)       | 0     |
| No security contact      | (none)       | 0     |
| **Total**                | --           | **0** |

### High-Risk Dependencies

The following dependencies have two or more risk factors.

| Dependency Name | Risk Factors | Notes | Suggested Alternative |
|-----------------|--------------|-------|-----------------------|
| (none)          | --           | No third-party dependencies were identified in this project. | -- |

## Suggested Alternatives

No alternatives are needed. The project has no high-risk dependencies.

## Recommendations

1. **Maintain the zero-dependency posture.** The absence of third-party dependencies is the strongest possible supply chain security stance. Continue relying on Apple first-party frameworks where possible.

2. **Pin the GitHub Actions checkout action to a full commit SHA.** The CI workflow currently uses `actions/checkout@v4` (a mutable tag). For defense-in-depth against tag hijacking attacks, consider pinning to a specific commit SHA, e.g.:
   ```yaml
   - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
   ```

3. **If third-party dependencies are added in the future**, commit `Package.resolved` to version control (currently gitignored) so that builds are reproducible and dependency versions are auditable. Alternatively, ensure the `.gitignore` exclusion is intentional and documented.

4. **The project's `SECURITY.md` is well-structured** with a clear disclosure policy, scope definition, and contact information. No changes needed.

## Report Generated By

Supply Chain Risk Auditor Skill
Generated: 2026-04-03
